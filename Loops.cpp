#include<iostream>
using namespace std;

int main(){
/*
    #ifndef ONLINE_JUDGE
        freopen("Input.txt","r",stdin);
        freopen("Output.txt","w",stdout);
    #endif
  */
    // sum of 1st N natural numbers.
    /*
    int N,sum=0;
    cin>>N;
    for (int i=1;i<=N;i++){  // in for loop counter is important i
        sum=sum+i;
    }
    cout<<sum;
    */


/*
   // using while loop  condition is important loop will run till contition is True.
   int N; // while loop will continue until negative number is entered.
   cin>>N;
   while (N>0){
       cout<<N;
       cin>>N;
}
*/

/*
       // do while loop is inversr of while loop
       // while loop 1st condition is checked then code inside body is executed.
       // do while loop 1st code inside body is executed then condition is checked.
       // do while is executed at least once.
    
    int N;
    cin>>N;
    do
    {
        cout<<N;
        cin>>N;
    } while (N>0);
    
 */


    return 0;
}